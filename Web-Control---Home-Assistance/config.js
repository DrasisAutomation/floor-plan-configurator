// config.js - WebSocket configuration only
const WS_URL = "wss://homeassistant.com/api/websocket"
const TOKEN = "live long token"

// Make them globally available
window.WS_URL = WS_URL
window.TOKEN = TOKEN
